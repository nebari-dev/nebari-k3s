name: vagrant-ha-security

on:
  pull_request:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  vagrant-tests:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        platform: [rocky9, ubuntu]
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Show environment
        run: |
          uname -a
          ruby -v || true
          vagrant --version || true

      # -------- Linux: full run with libvirt (preferred CI path) --------
      - name: Install virtualization deps (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update -y
          sudo apt-get install -y qemu-kvm libvirt-daemon-system libvirt-clients virt-manager dnsmasq-base nmap
          sudo usermod -aG libvirt $USER || true
          sudo systemctl enable --now libvirtd

          # Install Vagrant (if not present)
          if ! command -v vagrant >/dev/null 2>&1; then
            sudo apt-get install -y vagrant
          fi

          vagrant plugin install vagrant-libvirt

      # -------- macOS: validate-only by default (GitHub-hosted runners can lack HVF support) --------
      # HVF unsupported reports on macOS ARM64 runners are common :contentReference[oaicite:8]{index=8}
      - name: Install deps (macOS)
        if: runner.os == 'macOS'
        run: |
          brew update
          brew install vagrant qemu nmap || true
          vagrant plugin install vagrant-qemu || true

      - name: Platform env (prints what will be used)
        run: |
          bash tests/bin/env.sh

      - name: Validate Vagrantfile
        run: |
          cd tests/${{ matrix.platform }}
          vagrant validate

      # Full run on Linux
      - name: Bring up cluster + run Ansible + security scans (Linux)
        if: runner.os == 'Linux'
        run: |
          set -euxo pipefail
          source tests/bin/env.sh

          cd tests/${{ matrix.platform }}

          # For Rocky, add the correct box URL (from env.sh)
          if [[ "${{ matrix.platform }}" == "rocky9" ]]; then
            vagrant box add --name "${ROCKY_BOX_NAME}" "${ROCKY_BOX_URL}" --provider=libvirt || true
          fi

          vagrant up --provider="${PROVIDER}"

          cd -
          ansible-playbook -i tests/${{ matrix.platform }}/inventories/hosts.ini playbook.yaml
          bash tests/bin/security-scan.sh tests/${{ matrix.platform }}/inventories/hosts.ini

      # Optional full run on macOS only for self-hosted runners
      - name: Bring up cluster + run Ansible + scans (macOS self-hosted only)
        if: runner.os == 'macOS' && runner.name == 'self-hosted'
        run: |
          set -euxo pipefail
          source tests/bin/env.sh

          cd tests/${{ matrix.platform }}
          if [[ "${{ matrix.platform }}" == "rocky9" ]]; then
            vagrant box add --name "${ROCKY_BOX_NAME}" "${ROCKY_BOX_URL}" --provider=libvirt || true
          fi

          vagrant up --provider="${PROVIDER}"

          cd -
          ansible-playbook -i tests/${{ matrix.platform }}/inventories/hosts.ini playbook.yaml
          bash tests/bin/security-scan.sh tests/${{ matrix.platform }}/inventories/hosts.ini
