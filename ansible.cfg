[defaults]
# Quietly auto-detect the Python interpreter, silencing the "discovered Python
# interpreter" WARNING on hosts that ship a newer python3.x (e.g. Ubuntu 26.04).
interpreter_python = auto_silent
